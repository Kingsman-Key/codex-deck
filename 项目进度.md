# Codex Deck｜项目进度
更新日期：2026-09-24

## 目标与当前状态

目标是提供一个可以同时运行多个 Codex/ChatGPT 桌面窗口的本地总控台，避免 CC Switch 一类方案每次修改全局配置、退出和重启。v0.3.3 延续原 Codex、DeepSeek 直连、项目目录恢复和历史工具能力，并补充 Windows x64 安装版/免安装版构建链路；Windows 真实多实例运行仍待实机验证。

## 目录与文件索引

| 路径 | 功能与作用 | 状态 |
| --- | --- | --- |
| [AGENTS.md](AGENTS.md) | 长期维护、安全与验证规则 | 已建立 |
| [README.md](README.md) | 产品说明、使用方法、架构与调研取舍 | 已完成 |
| [CONTRIBUTING.md](CONTRIBUTING.md) | 公开项目的开发、验证与安全贡献规范 | 已完成 |
| [SECURITY.md](SECURITY.md) | 支持范围与私密漏洞报告流程 | 已完成 |
| [package.json](package.json) | Electron/React 工程、验证与打包脚本 | 已完成 |
| [package-lock.json](package-lock.json) | 可复现依赖锁定 | 已完成 |
| [index.html](index.html) | Renderer 入口与内容安全策略 | 已完成 |
| [tsconfig.json](tsconfig.json) | Renderer TypeScript 配置 | 已完成 |
| [tsconfig.electron.json](tsconfig.electron.json) | Electron 主进程 TypeScript 配置 | 已完成 |
| [vite.config.ts](vite.config.ts) | Renderer 生产构建配置 | 已完成 |
| [electron](electron/项目进度.md) | 本机 profile、原 Codex 入口、加密、CC Switch 导入、配置生成和进程控制 | v0.3 已完成 |
| [src](src/项目进度.md) | React 总控台、原有/独立历史提示、CC Switch 导入界面与共享类型 | v0.3 已完成 |
| [tests](tests/项目进度.md) | 配置、导入、安全、隔离、进程识别、项目目录恢复、历史迁移/导出/转移/上下文复制、增量同步和登录注入测试 | 30 项通过 |
| [buildResources](buildResources/项目进度.md) | 应用图标源文件及打包资源 | 已完成 |
| [.github](.github/项目进度.md) | GitHub Actions 与公开仓库协作配置 | 已完成 |
| `dist/` | 编译产物，不纳入业务索引 | 已生成 |
| `release/` | 本机打包产物，不纳入业务索引 | macOS arm64 v0.3.2 已生成；Windows x64 v0.3.3 安装版与 Portable 已交叉构建，待原生 CI 复核 |

## 里程碑与下一步

| 事项 | 状态 | 完成条件 / 当前依赖 |
| --- | --- | --- |
| GitHub 方案调研与许可证筛选 | 已完成 | 记录 6 个相关项目、星数、许可证和取舍；Cockpit Tools 仅借鉴产品结构，未复制其非商用许可代码 |
| macOS 多实例 MVP | 已完成 | 生产构建启动、主界面与新增配置弹窗完成视觉检查 |
| CC Switch 自动发现与导入 | 已完成 | 真实数据库只读发现 5 个 Codex 配置，其中 4 个符合安全规则可导入；未实际迁移用户凭据 |
| 核心自动化验证 | 已完成 | 类型检查、30 项测试、生产构建、打包与 0 个 npm 审计漏洞 |
| 公开 GitHub 仓库 | 已完成 | [Kingsman-Key/codex-deck](https://github.com/Kingsman-Key/codex-deck) 已公开，`main` CI 通过、私密漏洞报告已启用，v0.3.2 已附 macOS DMG/ZIP |
| 可安装 macOS 分发包 | 已完成（本地版） | DMG/ZIP 已生成且 DMG 校验有效；正式公开发布仍需签名与公证 |
| 真实双 Codex 窗口并发回归 | 待用户账号验证 | 需要用户在两个隔离窗口分别完成登录并确认会话互不串号 |
| Windows 实机支持 | 安装包已生成，运行待验证 | 已配置并交叉构建 NSIS/Portable x64；待原生 Windows CI 复核和实机验证安装发现、窗口聚焦、退出与真实多开 |
| 托盘与跨重启进程恢复 | macOS 已完成；Windows 待开发 | macOS 启动时扫描运行中进程并按 `--user-data-dir` 回填；Windows 关闭总控台后仍需保留 PID/托盘管理 |
| 按 provider 迁移旧历史 | 已完成 | 只复制原 `~/.codex` 中 `model_provider` 匹配的会话与消息库到目标 profile，不修改原目录 |
| 历史导出与跨 provider 转移 | 已完成 | 可导出可读 Markdown/JSONL 副本，或复制到另一个 provider 并改写目标 provider 标签 |
| 最新上下文一键交接 | 已完成 | 读取 A 最新会话、复制到剪贴板并切到 B，再由用户粘贴 |
| 启动自动同步最新上下文 | 已完成 | profile 可选在 Codex Deck 启动时复制当前 Codex 最新上下文并打开该 profile，默认关闭 |
| 主库到 profile 增量同步 | 已完成 | 按 provider 标签把主库新增线程合并到 profile，保留目标已有线程；支持手动和启动自动执行 |
| CC Switch 导入同步策略 | 已完成 | 非 ChatGPT provider 默认启用 `autoSync=history`；ChatGPT/OpenAI 多账号不自动合并，避免同 tag 串历史 |
| CC Switch 登录态重新注入 | 已完成 | 导入 profile 可按来源 ID 从 CC Switch 重新写入 OAuth `auth.json` 或 API Key，不需要 Safari 回调 |
| 用量与额度看板 | 待评估 | 必须基于公开稳定接口，不能读取或记录敏感令牌内容 |

## 变更记录

| 日期 | 涉及路径 | 变动与原因 | 验证 / 剩余问题 |
| --- | --- | --- | --- |
| 2026-09-23 | 全项目 | 从空目录初始化 Codex Deck；合并独立 `CODEX_HOME` 和 Electron 用户目录隔离两类方案 | `npm run verify` 通过；生产 `.app` 成功启动 |
| 2026-09-23 | `package.json`、依赖 | Electron 升级到 44.4.3、Vitest 升级到 5.0.1，消除已知审计项 | `npm audit --audit-level=moderate` 为 0 漏洞 |
| 2026-09-23 | `src/`、`electron/` | 完成总控台、profile CRUD、系统加密、provider TOML、进程启动/聚焦/退出 | 8 项自动化测试通过；UI 生产截图检查通过 |
| 2026-09-23 | `buildResources/`、`release/` | 新增自有品牌 SVG、PNG 与 macOS ICNS，并生成本地分发包 | 重打包后真实启动成功；DMG 通过 `hdiutil verify` |
| 2026-09-23 | `electron/cc-switch-importer.ts`、`src/`、`tests/` | 增加 CC Switch 数据库自动发现、脱敏预览、批量导入和来源去重；按认证类型隔离迁移凭据 | 真实数据只读扫描通过；10 项自动化测试通过；主界面和导入弹窗完成生产截图检查 |
| 2026-09-23 | `package.json`、`release/` | 版本升级为 v0.2.0，并重新生成 macOS arm64 DMG/ZIP | 应用版本核对为 0.2.0；DMG 通过 `hdiutil verify`；发布包未签名、未公证 |
| 2026-09-23 | `.github/`、`README.md`、`CONTRIBUTING.md`、`SECURITY.md` | 补齐公开 GitHub 项目的 CI、下载入口、贡献指南和私密安全报告流程 | 本地发布前检查通过；首次远程 CI 待仓库推送后确认 |
| 2026-09-23 | GitHub `Kingsman-Key/codex-deck` | 创建公开仓库、推送 `main`、设置项目主题、启用 Private Vulnerability Reporting 并发布 v0.2.0 Public Beta | 两轮 GitHub Actions CI 成功；源码归档已公开，DMG/ZIP 因大文件上传链路异常缓慢暂未附加，仍保留本地验证产物 |
| 2026-09-23 | `electron/`、`src/`、`tests/`、`README.md` | 针对旧聊天不可见和 API 窗口再次要求登录的问题新增“当前 Codex”原入口、隔离历史标识和缺失桌面认证补全；调研 Cockpit Tools 的默认/独立实例结构并独立实现 | 类型检查与 14 项测试通过；v0.3 生产包真实启动和界面截图检查通过 |
| 2026-09-23 | `package.json`、`release/` | 版本升级为 v0.3.0，并重新生成 macOS arm64 DMG/ZIP | 应用版本核对为 0.3.0；DMG 通过 `hdiutil verify`；发布包未签名、未公证 |
| 2026-09-23 | `electron/launcher.ts`、`electron/main.ts`、`tests/launcher.test.ts`、进度文档 | 增加 macOS 跨重启进程恢复：启动时只读扫描 `ps`，按原生入口与 `--user-data-dir` 区分实例，重新聚焦已运行窗口，避免 Codex Deck 重启后重复启动 | 类型检查与 16 项测试通过；生产构建与打包检查通过 |
| 2026-09-23 | `electron/history-migration.ts`、`electron/main.ts`、`src/App.tsx`、`src/shared/types.ts`、`tests/history-migration.test.ts` | 增加按 provider 迁移历史：从当前 Codex 只读快照，复制匹配的 `state_5.sqlite` 线程、JSONL 会话和 `thread_history_1.sqlite` 消息到目标 profile；目标已有记录时拒绝覆盖 | 类型检查与 18 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `electron/history-migration.ts`、`electron/main.ts`、`electron/preload.ts`、`src/App.tsx`、`src/shared/types.ts`、`tests/history-migration.test.ts` | 增加历史工具弹窗：从当前 Codex 迁移、导出可读副本、把 A provider 历史转移到 B provider；转移时改写目标 `model_provider` | 类型检查与 20 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `electron/history-migration.ts`、`electron/main.ts`、`electron/preload.ts`、`src/App.tsx`、`src/shared/types.ts`、`tests/history-migration.test.ts` | 增加“复制上下文并切到目标”：读取来源最新会话，写剪贴板后聚焦/启动目标窗口，避免不可靠的自动键盘注入 | 类型检查与 21 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `electron/history-migration.ts` | 修复历史转移后目标 SQLite 因残留 `-wal/-shm/-journal` 报 malformed 的问题；已实际把当前 DeepSeek 会话复制进 FabiMader 并重新打开 | 类型检查与 21 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `electron/main.ts`、`electron/profile-store.ts`、`src/App.tsx`、`src/shared/types.ts`、`tests/profile-store.test.ts` | 增加 profile 级“启动时复制最新上下文并打开目标”设置，Codex Deck 启动时自动执行，默认关闭 | 类型检查与 22 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `electron/history-migration.ts`、`electron/main.ts`、`electron/preload.ts`、`src/App.tsx`、`src/shared/types.ts`、`tests/history-migration.test.ts` | 增加主库到 profile 的增量同步：按 `thread_id` 只补新增会话，保留目标已有记录；可手动执行或设置启动自动同步 | 类型检查与 23 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `electron/profile-store.ts`、`electron/main.ts`、`tests/profile-store.test.ts` | 自动历史同步只对非 ChatGPT provider 默认开启；OpenAI 多账号保持独立，避免同 `openai` tag 串历史 | 类型检查与 24 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `electron/cc-switch-importer.ts`、`electron/main.ts`、`electron/preload.ts`、`src/App.tsx`、`src/shared/types.ts`、`tests/cc-switch-importer.test.ts` | 增加“从 CC Switch 重新注入登录态”：按来源 ID 重写目标隔离 `auth.json` 或加密 API Key，避免依赖 Safari OAuth 回调 | 类型检查与 25 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `electron/profile-runtime.ts`、`electron/profile-store.ts`、`electron/cc-switch-importer.ts`、`electron/validation.ts`、`src/`、`tests/` | 修复 DeepSeek profile 被带入登录流程：直连官方 Responses API，迁移旧模型 ID，停用不兼容的 CC Switch 模型目录；缺少登录文件时单次复制当前认证供桌面壳使用，模型请求仍由 DeepSeek Key 路由 | 类型检查与 28 项测试通过；含当前文件型认证的临时 profile 使用虚拟 Key 时仍直接请求 `api.deepseek.com`，证明桌面认证不会覆盖 provider；未读取或输出真实 Key |
| 2026-09-24 | `package.json`、`release/`、生产应用 | 版本升级为 v0.3.1 并重新生成 macOS arm64 DMG/ZIP；用隔离 `--user-data-dir` 真实启动打包应用，检查主界面、四项 provider 两列布局和 DeepSeek 官方表单 | 生产界面截图复核通过；应用版本 0.3.1，DMG 校验有效，未签名、未公证 |
| 2026-09-24 | `electron/project-state-sync.ts`、`electron/main.ts`、`tests/profile-runtime.test.ts`、`README.md` | 修复 DeepSeek/隔离窗口只有最近聊天、原有项目文件夹不显示：选择性同步项目目录和侧边栏状态，复用已存在的项目数据库映射，仅合并目标已有线程归属并先备份目标全局状态 | 类型检查与 30 项测试通过；检测到原 33 个项目及 43 个目录根仍完整存在，真实数据只读副本恢复 33 个项目/33 条映射/9 条目标线程归属；未改写运行中的 DeepSeek 实例 |
| 2026-09-24 | `package.json`、`release/` | 版本升级到 v0.3.2，生成 macOS arm64 `.app`、DMG 和 ZIP | 应用版本核对为 0.3.2；DMG 校验有效；未签名、未公证 |
| 2026-09-24 | GitHub `v0.3.2` | 推送项目恢复修复并发布公开预览版，附 Apple Silicon DMG/ZIP | `main` CI 成功；GitHub 资产 SHA-256 与本地产物一致 |
| 2026-09-24 | `package.json`、`buildResources/icon.ico`、`.github/workflows/windows-build.yml`、`README.md` | 版本升级为 v0.3.3，增加 Windows x64 NSIS 安装版、Portable 免安装版、品牌 ICO 和原生 CI artifact | 30 项测试、类型检查、生产构建与审计通过；macOS 交叉构建两个约 107 MB 的 EXE 成功，内部主程序为 PE32+ x86-64；Windows runner 与实机运行待验证 |
