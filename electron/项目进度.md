# electron｜项目进度
更新日期：2026-09-24 · [返回上级](../项目进度.md)

## 职责与状态

负责 Electron 主进程、本地 profile 元数据、系统加密、CC Switch 只读发现与导入、Codex TOML 生成、原 Codex/独立运行目录、桌面程序发现、进程启动/聚焦/退出与安全 IPC。macOS v0.3 已完成。

## 文件与子目录

| 路径 | 功能与作用 | 状态 |
| --- | --- | --- |
| [项目进度.md](项目进度.md) | 本目录索引与验证记录 | 已建立 |
| [main.ts](main.ts) | 应用生命周期、窗口、IPC 和各服务编排 | 已完成 |
| [preload.ts](preload.ts) | 最小化、类型化的 Renderer IPC 桥 | 已完成 |
| [profile-store.ts](profile-store.ts) | profile 与加密 secret 分离存储、受保护的原 Codex 入口、原子 JSON 写入 | 已完成 |
| [main.ts](main.ts) | 应用生命周期、窗口、IPC、启动时自动上下文同步编排 | 已完成 |
| [profile-runtime.ts](profile-runtime.ts) | 原 Codex 路径、隔离运行目录、config、共享 skills/plugins、API profile 桌面认证补全 | 已完成 |
| [cc-switch-importer.ts](cc-switch-importer.ts) | 只读扫描 CC Switch SQLite、脱敏预览、配置净化与隔离导入 | 已完成 |
| [config-generator.ts](config-generator.ts) | 生成 ChatGPT 或 Responses provider TOML | 已完成 |
| [history-migration.ts](history-migration.ts) | 按 provider tag 迁移/增量同步历史、导出可读副本、跨 provider 转移、生成最新上下文 | 已完成 |
| [launcher.ts](launcher.ts) | 程序发现、环境清理、原 Codex/独立启动、跨重启进程识别、窗口聚焦与退出 | macOS 已验证；Windows 待实机 |
| [validation.ts](validation.ts) | profile 名称、颜色、模型与安全 URL 校验 | 已完成 |

## 详细变更与验证

| 日期 | 文件 / 事项 | 变动与原因 | 验证与当前状态 |
| --- | --- | --- | --- |
| 2026-09-23 | 全目录 | 建立 profile 生命周期、隔离与加密主进程 | TypeScript 严格检查通过 |
| 2026-09-23 | `launcher.ts` | 启动时覆盖 `CODEX_HOME`/`CODEX_SQLITE_HOME`，传递独立 `--user-data-dir`，清除继承凭据 | Launcher 单元测试通过；macOS 应用真实启动通过 |
| 2026-09-23 | `config-generator.ts` | 为 custom provider 固定 Responses 协议，TOML 不写明文 Key | 配置生成测试通过 |
| 2026-09-23 | `cc-switch-importer.ts`、`profile-store.ts`、`profile-runtime.ts` | 导入 CC Switch Codex provider，按 API Key/OAuth 分流凭据，保留安全配置与模型目录，并记录来源防重复 | 真实数据库只读扫描为 5 个候选、4 个可导入；导入器测试通过，未实际导入用户凭据 |
| 2026-09-23 | `main.ts`、`preload.ts` | 新增扫描和导入 IPC，并按 profile 认证类型判断启动条件 | Electron TypeScript 检查和打包通过 |
| 2026-09-23 | `profile-store.ts`、`profile-runtime.ts`、`launcher.ts`、`main.ts` | 新增不可删除的“当前 Codex”入口，原地使用 `~/.codex` 保留旧历史；API 实例缺少桌面认证时复制当前文件型 auth 且不覆盖已有认证 | 相关单元测试通过；macOS v0.3 打包与真实启动验证通过 |
| 2026-09-23 | `launcher.ts`、`main.ts` | 新增 macOS 跨重启进程恢复：启动时扫描 `ps`，按原生入口和 `--user-data-dir` 回填 PID；点击时也能直接聚焦已存在的隔离实例 | 类型检查与 16 项测试通过；生产构建与打包检查通过 |
| 2026-09-23 | `history-migration.ts`、`main.ts`、`preload.ts` | 新增按 provider 历史迁移 IPC：对当前 `~/.codex` 做 SQLite 备份快照，复制匹配线程、会话文件与 `thread_history` 消息到隔离目录，目标非空时拒绝覆盖 | 类型检查与 18 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `history-migration.ts`、`main.ts`、`preload.ts` | 新增历史导出与跨 provider 转移：导出为 Downloads 下的 Markdown/JSONL 目录，或把 A 的匹配历史复制到 B 并改写 `model_provider` 标签 | 类型检查与 20 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `history-migration.ts`、`main.ts`、`preload.ts` | 新增最新上下文交接：读取来源最近会话消息，写入 Electron 剪贴板后聚焦/启动目标窗口，不进行自动键盘注入 | 类型检查与 21 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `history-migration.ts` | 替换目标 SQLite 前清理 `-wal/-shm/-journal` sidecar，避免真实转移时出现 malformed | 类型检查与 21 项测试通过；真实 FabiMader 转移验证通过 |
| 2026-09-24 | `main.ts`、`profile-store.ts` | 增加启动自动上下文同步：profile 保存 `autoSync=context` 时，启动后复制当前 Codex 最新上下文并打开目标；目标运行中则跳过 | 类型检查与 22 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `history-migration.ts`、`main.ts`、`preload.ts` | 增加主库到 profile 的增量历史同步：按 thread id 只补新增线程并合并 thread_history；目标已有记录不删除；支持 `autoSync=history` 和手动 IPC | 类型检查与 23 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `profile-store.ts`、`main.ts` | CC Switch 导入的非 ChatGPT provider 默认启用历史自动同步；ChatGPT/OpenAI 多账号保持独立，避免同 tag 串历史 | 类型检查与 24 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `cc-switch-importer.ts`、`main.ts`、`preload.ts` | 增加 CC Switch 登录态重新注入：按来源 ID 从只读数据库重写目标 OAuth `auth.json` 或加密 API Key | 类型检查与 25 项测试通过；生产构建与打包检查通过 |
| 2026-09-24 | `validation.ts`、`config-generator.ts`、`cc-switch-importer.ts` | 增加 DeepSeek 官方 Responses preset；识别 `api.deepseek.com`，净化旧 `model_catalog_json` 和当前客户端不支持的登录配置；来源模型目录仍作为副本留存 | 单元测试覆盖官方 URL、模型 ID 与配置净化；未修改 CC Switch 数据库 |
| 2026-09-24 | `profile-store.ts`、`profile-runtime.ts`、`main.ts` | 启动时把旧 DeepSeek profile 升级到 `deepseek-flash`；API profile 缺少认证时单次复制当前 `auth.json` 供桌面壳使用，provider 路由继续使用加密 API Key | 28 项测试通过；含本地认证的临时 profile 使用虚拟 Key 时仍直连 `api.deepseek.com`；当前运行窗口保留到用户自行关闭 |

## 下一步

补充真实迁移后的会话打开回归、Windows 托盘驻留与实机窗口聚焦回归；正式分发前增加签名、公证和更新机制。
